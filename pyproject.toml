[project]
name = "air-quality-be"
version = "0.1.0"
description = "FastAPI service for ingestion, analytics and forecasting"
requires-python = ">=3.11"
dependencies = [
  "asyncpg>=0.30.0",
  "fastapi>=0.115.0",
  "uvicorn[standard]>=0.30.0",
  "pydantic>=2.9.0"
]

[tool.uvicorn]
factory = false
